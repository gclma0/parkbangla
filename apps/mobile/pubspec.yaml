name: parkbangla_mobile
description: ParkBangla — peer-to-peer parking, Commuter Pass
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  parkbangla_client:
    path: ../../packages/api_client
  google_fonts: ^6.2.1
  flutter_animate: ^4.5.0
  flutter_map: ^6.1.0
  latlong2: ^0.9.1
  shared_preferences: ^2.2.3
  qr_flutter: ^4.1.0
  intl: ^0.19.0
  firebase_core: ^4.7.0
  firebase_messaging: ^16.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
